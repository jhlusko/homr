# Detector B4 handoff — 2026-09-25, 04:17 UTC

This follows `HANDOFF_2026-09-24.md` and the accepted private sequence at
`../OurTextScores/docs/private/OTS_HOMR_RELEASE_SEQUENCE_2026-09-25.md`.
The earlier handoff's most important instance rule still applies: **use
`/workspace/venv`; never rebuild it**. The instance filesystem may disappear on
recycle/destroy; preserve valuable results off-box. Read
`/etc/vast-agents-guide.md` before acting on the instance.

## Live job and immediate next step

The four-epoch full-model B4 run has finished training. Its **selection-only**
real-page evaluations are running in persistent tmux session
`detector_b4_selection`, with `watch_detector_b4_selection` watching it. They
started at 04:13:46 UTC. At 04:16:49 UTC epoch 2 was still underway; epoch 1
had completed. Expected completion is approximately **04:30–04:35 UTC**,
subject to evaluation throughput. The recovery script ends at selection and
does **not** run the test split. No candidate test has been read.

Connect with:

```sh
ssh -F /dev/null -o BatchMode=yes -p 40097 root@88.207.87.60
```

Then inspect:

```sh
tmux list-sessions
tail -n 30 /workspace/detector-b4-full/progress.log
tail -n 30 /workspace/detector-b4-full/selection/epoch-2.log
ls -l /workspace/detector-b4-full/selection/epoch-*.json
test -e /workspace/detector-b4-full/SELECTION_DONE && echo selection-done
test -e /workspace/detector-b4-full/test.json && echo TEST-WAS-READ
```

The active script is `/workspace/detector-b4-full/finish_selection.sh` in the
clean worktree `/workspace/detector-b4-ossq-checkout` at pushed commit
`f071c15`. Read the script, progress log and `selection/decision.json` before
restarting anything. An older `/workspace/detector-b4-full/run.sh` contains a
test step and **must not** be restarted blindly. The prior tmux job was
intentionally stopped after epoch 1; `PAUSED_FOR_RESUME` records why.

## What was decided and measured

B2 folded Expression/Tempo/StaffText into `DirectionText` with a per-model
class-order manifest. That matters because runtime `homr/text_detection.py`
and box evaluation otherwise mislabel shifted channels. B3 froze a
score-disjoint real-page selection/test split in
`training/ocr/real_page_release_split_20260925.json`, with gate details in
`docs/DETECTOR_REAL_PAGE_RELEASE_GATE_2026-09-25.md` (commits `223f012`,
`e1223f4`, `9c99971`). All scores are from the 2026-09-19 audit's heldout
side, disjoint from its train scores. IoU 0.5 defines a box match; **recall**
is the selection/release metric, with a lower bound on precision via prediction
caps.

| Split | OSSQ Direction GT | OSSQ Dynamic GT | Lieder Direction GT |
| --- | ---: | ---: | ---: |
| Selection | 467 (145 pages) | 1,841 | 11 (12 pages) |
| Test | 421 (154 pages) | 1,660 | 11 (14 pages) |

Frozen selection floors: OSSQ Direction **109/467**, OSSQ Dynamic **331/1841**,
Lieder Direction **7/11**. Among eligible epochs rank OSSQ Direction recall,
then Dynamic recall, then earlier epoch; synthetic history is a veto. The
parent is fallback. Frozen test/release floors: OSSQ Direction **105/421**,
Lieder Direction **11/11**, OSSQ Dynamic **1018/1660**. Prediction caps are
OSSQ Direction **367**, Lieder Direction **87**, and OSSQ Dynamic **5728**;
synthetic history must pass. Do not tune these using test results.

Baselines are in `/workspace/detector-b3-baselines/{parent,e4}.json` and the
local archive under
`../homr-artifacts/gpu-roadmap-20260919/instance-results/workspace/detector-b3-baselines/`.
The five-class parent scored selection OSSQ Direction 94/467, Dynamic 367/1841,
Lieder Direction 8/11; test OSSQ Direction 83/421, Dynamic 465/1660, Lieder
Direction 11/11. Released e4 scored selection OSSQ Dynamic 1169/1841 and test
Dynamic 1101/1660. SHA-256: parent weights
`a03cd63177353360faf7c8a377e061d5130c3eeeab10775fd2c980a08096520c`,
e4 weights `fcb8ca529fa355b0427112bda1c4edd95b2d6cc6c369be0c32f3313987a4ec82`,
split manifest `43f922bc24179a2a182888df54587514db9da4ac0a806ad242c0e8e361fad72f`.

The first B4 **head-only** pilot (commit `25f2922`) trained four epochs on 60
Lieder pages plus synthetic patches, fixing trunk and non-direction channels.
All four selection epochs gave **0/467 OSSQ Direction and 0/11 Lieder
Direction**, though Dynamic was 1159/1841. It has no eligible checkpoint;
test was not run. See `/workspace/detector-b4-pilot/selection/decision.json`.

For the second B4 run, the OCR-confirmed OSSQ annotation archive was audited
against actual score/page IDs, not JSON filenames. Of 93 documents, 69 audited
train scores were usable; 11 nominally train-adjacent scores were older heldout,
one was outside the audit, 709 box matches crossed score boundaries, and 211
matched missing teaser images. Preparation commits `2ca90be`, `479a256`.
`/workspace/detector-b4-ossq-real/prepared/audit.json` records **1,840 pages,
5,215 DirectionText boxes, 22,140 Dynamic boxes**. It uses blank paper
background and ignores unlabelled ink. The bank has 29,440 patches (16/page,
half positive), at `/workspace/detector-b4-ossq-real/bank/index.txt`. Source
JSONs are in `/workspace/detector-b4-ossq-real/matches/`; scan images were
already on the instance. Audit, index and logs are archived locally under
`../homr-artifacts/gpu-roadmap-20260919/instance-results/workspace/detector-b4-ossq-real/`.

The full-model run (commit `4953d0d`) started from remapped e4, trained **all
parameters**, mixed 40% OSSQ real / 20% Lieder real / 40% synthetic, and used
16,384 samples/epoch, batch 32, AdamW 2e-5. Four epochs finished at 04:04:21
UTC; weights and history are under `/workspace/detector-b4-full/weights/`.
Synthetic epoch-4 IoU was Dynamic .8174, DirectionText .8463, Lyrics .8999;
synthetic scores do not decide release. Real-page **epoch 1** selection was
OSSQ Direction **81/467** with 706 predictions, Dynamic **1159/1841** with
15,152 predictions, Lieder Direction **8/11** with 207 predictions. It clears
the Lieder and Dynamic recall floors but misses the OSSQ Direction floor.
Epochs 2–4 are being scored now. No release or pin has changed.

## Resuming and deciding what to train next

User asked to permit extra epochs if the real-page trend improves. Commit
`f071c15` added atomic `trainer-state.pth` after each future epoch with model,
AdamW optimizer, sampler generator, torch/CUDA/NumPy/Python RNG, history, and
metadata checks. `--resume` gives an exact continuation from that state;
`--start-weights` plus `--history-in` gives a warm restart from old weights.
Four focused tests passed. **The current four-epoch run predates this feature
and has no optimizer/RNG state.** Extending its epoch-4 checkpoint is thus a
weight-only warm restart; AdamW and RNG reset. An exact continuation would
require rerunning epochs 1–4 using the new code.

When `SELECTION_DONE` appears, read all four selection reports and the
decision. Compare matched/GT **and prediction counts** for OSSQ Direction,
Dynamic, and Lieder Direction. If OSSQ Direction is rising through epoch 4
while the other selection floors remain satisfied, continue for a few epochs
from epoch 4 in a **new output directory**. Use the full argument set in
`/workspace/detector-b4-full/run.sh`, changing output to a new directory and
adding `--start-weights /workspace/detector-b4-full/weights/epoch-4.pth`
and `--history-in /workspace/detector-b4-full/weights/history.json`, with
`--epochs 8` as the *total* target. The resulting epoch-5 onward checkpoints
will have exact-resume state. If the trend has plateaued or declined, do not
extend merely because training is cheap. Evaluate any extension checkpoints
on **selection only** using the same pages/manifest/role as
`finish_selection.sh`, and compare all epochs with
`training.ocr.select_direction_epoch`.

Only after choosing one final checkpoint, score it **once** on `--role test`
with `training.ocr.eval_folded_directions`, then apply the frozen gate using
`training/ocr/decide_direction_release.py` (commit `153b4ae`) with the
parent/e4 baselines, selection decision, candidate test report and history.
If it fails, keep current released e4 and the parent diagnostic; do not change
pins or make a public quality claim. If it passes, present the B5 release
decision before promoting/pinning. Save the final reports, logs and any
selected weights off-box with SHA-256 checks. The local archive contains
reports for the pilot and the OSSQ prep, but **not** all full-model weights.

The relevant planning sources are the private release sequence and roadmap,
`RUNLOG.md`, and the local
`../homr-artifacts/gpu-roadmap-20260919/STATUS.md`. The homr checkout has
unrelated pre-existing changes in `training/ocr/box_label_server.py`,
`.cache/`, `artifacts-instance/rareNum-heads-mixed/`, and `uv.lock`; do not
stage or overwrite them during this work.
